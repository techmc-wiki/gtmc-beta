/** Height in pixels of the sticky navbar at the top of the viewport */
export const NAVBAR_HEIGHT = 64

/** Time in milliseconds before the article label morphs after scroll stops */
export const LABEL_MORPH_DELAY_MS = 250

/** CSS media query string that matches desktop-sized viewports */
export const DESKTOP_MEDIA_QUERY = '(min-width: 768px)'
